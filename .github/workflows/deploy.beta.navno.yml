name: Deploy to Team nav.no beta
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    uses: ./.github/workflows/build-and-deploy.yml
    permissions:
      id-token: write
      contents: read
    with:
      NAIS_VARS: .nais/vars/dev-beta-navno.yml
      IMAGE_NAME: decorator-next
      CLUSTER: dev-gcp
    secrets: inherit

