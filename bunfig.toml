[install]
# Only install package versions published at least 3 days ago
minimumReleaseAge = 259200
minimumReleaseAgeExcludes = ["@navikt/nav-dekoratoren-moduler"]

[install.scopes]
"@navikt" = { token = "$NODE_AUTH_TOKEN", url = "https://npm.pkg.github.com" }
