{
    "name": "decorator-shared",
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "lint": "eslint . && tsc --noEmit"
    },
    "dependencies": {
        "decorator-icons": "workspace:*"
    }
}
